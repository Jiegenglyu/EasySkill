interface:
  display_name: "Fake Human UI Recorder"
  short_description: "Record flows and compile replay specs"
  default_prompt: "Use $api-replay-recorder to record a website operation once, preserve UI and network evidence, write a semantic operation spec, check runtime fit, and only finalize materials after accepted replay."
